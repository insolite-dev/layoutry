name: layoutry
description: A simple layout information builder for your Flutter project.
version: 1.0.0
homepage: https://github.com/insolite-dev/layoutry

environment:
  sdk: '>=2.18.2 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
